# wifi-weevil
WiFi-Weevil is a Python program that executes a deauthentication attack on a client given an access point, then intercepts and captures the following WPA2 handshake. Using tools such as hashcat, the handshake can then be cracked to determine the WiFi password.
